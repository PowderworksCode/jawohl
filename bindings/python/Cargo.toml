# @generated by jedem for the `python` target. Do not edit.
# Surface `jawohl` v0.2.0. Regenerate with `cargo jedem generate`.

[package]
name = "jawohl-python"
description = "The Python binding for `jawohl`, generated by jedem."
version = "0.2.0"
edition = "2021"
publish = false

[lib]
# Must match the `#[pymodule]` function, and the file Python imports.
name = "jawohl"
crate-type = ["cdylib"]

[dependencies]
jawohl = { path = "../.." }
pyo3 = { version = "0.23", features = ["extension-module"] }
